node_modules/
.expo/
dist/
web-build/
ios/

# Android build outputs and local config — never commit these
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/keystore.properties

# Signing — never commit these
*.jks
*.p8
*.p12
*.key
*.mobileprovision
release/keystore.properties
release/ufcl-release.jks

# Environment files — store securely, not in git
.env
.env.local
.env.development
.env.staging
.env.production

# OS
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*
.DS_Store
